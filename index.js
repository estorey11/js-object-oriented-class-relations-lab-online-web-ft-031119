
let driverId=0
class Driver{
   constructor(name) {
        this.id = ++driverId;
        this.name = name;
 
        // insert in the user to the store
        //store.users.push(this);
    }
}

let passengerId=0
class Passenger{
   constructor(name) {
        this.id = ++driverId;
        this.name = name;
 
        // insert in the user to the store
        //store.users.push(this);
    }
}








